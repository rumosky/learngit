---
title: Git报错fatal refusing to merge unrelated histories
author: AndyBin
date: 2019-03-01
isOriginal: true
description: 用于记录Git报错fatal refusing to merge unrelated histories的原因以及解决办法
---

### 说明

::: warning

有时在 pull 或 merge 时会出现错误：`fatal: refusing to merge unrelated histories`.

解决办法：

```shell
git merge origin/master --allow-unrelated-histories
```

结果如下：

```shell
$ git merge origin/master --allow-unrelated-histories
Already up to date!
Merge made by the 'recursive' strategy.
```

只需要在命令的最后面添加`--allow-unrelated-histories`，这句话是告诉 Git 允许不相关历史合并。

:::
